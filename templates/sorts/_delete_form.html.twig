<form method="post" action="{{ path('app_sorts_delete', {'id': dnd35_sort.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dnd35_sort.id) }}">
    <button class="btn">Delete</button>
</form>
